{
  "name": "workout-app",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "test": "node test-v3.js",
    "test:smoke": "playwright test e2e/",
    "validate-demo-links": "node scripts/validate-demo-links.js"
  },
  "dependencies": {
    "jsdom": "^29.0.2"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.0"
  }
}
